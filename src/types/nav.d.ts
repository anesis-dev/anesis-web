/**
 * Navigation item type.
 *
 * Used by `docsNav`, `nav`, and `accountMenu` constants to define links
 * rendered in the header, docs sidebar, and account menus.
 */
export interface INav {
  title: string;
  url: string;
}
